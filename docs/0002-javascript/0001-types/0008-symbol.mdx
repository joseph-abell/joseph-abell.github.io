# Symbol

A symbol is a unique value. Can be useful for IDs. I've never knowingly used this.


```JS
const a = Symbol();
const b = Symbol();
const c = Symbol('c');
const d = Symbol('c');
a === b // false
c === d // false

```